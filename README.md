# golang-app-and-package
Test repo to figure out how to make a golang repo that includes a package and that the package should only upgrade if the package code is changed and not the rest of the repo.
